{
  "name": "@spark901/meeting-summary-schema",
  "version": "0.1.0",
  "private": true,
  "description": "Typed JSON schema + validator for a civic-archive meeting summary (agenda items, action items, topics, transcript caveat). Shared contract between whatever generates summaries (a Bedrock batch job, or a hand-written reference set) and any site that renders them. Not published to a registry — no build step; consumers import the TypeScript source directly.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
